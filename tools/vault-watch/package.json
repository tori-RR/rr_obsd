{
  "name": "obsidian-vault-watch",
  "version": "0.1.0",
  "private": true,
  "description": "Native Windows change notifications for Obsidian vaults on SMB and NAS storage.",
  "main": "main.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check src/main.js && node --check lib/native-bridge.js && node --check lib/reconciler.js && npm test && npm run build",
    "package": "npm run build && powershell.exe -NoProfile -NonInteractive -File scripts/package.ps1"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "esbuild": "0.28.2" },
  "license": "MIT"
}
